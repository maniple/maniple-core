<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>{{ translate(message) }}</title>
    <style>
        * {
            font-family: sans-serif;
        }
        .excl {
            font-size: 300px;
            font-weight: bold;
            text-align: center;
            line-height: 1;
        }

        .message-container-outer {
            width: 100%;
        }
        .message {
            text-align: center;
            font-size: 32px;
        }
        .message a {
            color: inherit;
            text-decoration: underline;
        }
        .message a:hover,
        .message a:focus,
        .message a:focus:hover {
            text-decoration: none;
        }

        pre {
            display: block;
            margin: 0 auto;
            padding: 10px;
            font-family: monospace, monospace;
            font-size: 12px;
            text-align: left;
            background: #ccc;
            color: #000;
            overflow: hidden;
            max-width: 960px;
            word-wrap: break-word;
            word-break: break-all;
            white-space: pre-wrap;
        }
    </style>
</head>
<body>

<div class="excl">!!!</div>

<div class="message-container-outer">
    <div class="message-container-inner">
        <div class="message">
            <p>{{ translate(message) }}</p>
            {% if exception %}
                <pre>{{ exception }}</pre>
            {% endif %}
            <p style="font-size:80%"><a href="{{ baseUrl('/') }}">{{ translate('Back to homepage') }}</a></p>
        </div>
    </div>
</div>
</body>
</html>
